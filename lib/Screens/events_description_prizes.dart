import 'package:flutter/material.dart';

class EventDescriptionPrizes extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text("Event Descripiton Prizes"),
    );
  }
}
